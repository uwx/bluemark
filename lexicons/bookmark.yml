lexicon: 1
id: io.github.uwx.bluemark.encryptedBookmark
defs:
  main:
    type: record
    description: An encrypted bookmark.
    key: tid
    record:
      type: object
      required:
        - salt
        - iv
        - cipher
      properties:
        salt:
          type: string
        iv:
          type: string
        cipher:
          type: string